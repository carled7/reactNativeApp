import React from "react";
import { View, Text } from "react-native";

export const UpdateFood = () => {
    return(
    <View style={{flex:1, alignItems: 'center', justifyContent: 'center'}}>
        <Text>UpdateFood</Text>
    </View>
    );
}