import React from "react";
import { View, Text } from "react-native";

export const UpdateWater = () => {
    return(
    <View style={{flex:1, alignItems: 'center', justifyContent: 'center'}}>
        <Text>UpdateWater</Text>
    </View>
    );
}